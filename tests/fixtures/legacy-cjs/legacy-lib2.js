// a second, distinct legacy lib - kept separate from legacy-lib.js so the
// dynamic-entry-point test below isn't affected by the other fixture's
// already-cached detection result
window.LegacyLibGlobal2 = 'legacy-value-2';
